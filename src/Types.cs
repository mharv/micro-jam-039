namespace Types;
public enum GamePhase { Menu, Round, Transition };
public enum Difficulty
{
    Easy,
    Medium,
    Hard
}


enum EnemyAttackType
{
    Spiral,
    FastBurst,
    LargeProjectile,
    Shotgun
}